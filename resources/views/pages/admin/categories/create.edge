@component('components/app', { title: 'Créer une catégorie - Admin' })
  <div class="max-w-2xl mx-auto px-6 py-10">
    <div class="mb-8">
      <a href="/admin/categories" class="inline-flex items-center gap-2 text-slate-600 hover:text-slate-900 mb-4 transition-colors">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="15 18 9 12 15 6"/></svg>
        Retour aux catégories
      </a>
      <h1 class="text-3xl font-extrabold text-slate-900 tracking-tight mb-2">Nouvelle Catégorie</h1>
      <p class="text-slate-500 font-medium">Créez une nouvelle catégorie pour organiser vos cours.</p>
    </div>

    <div class="bg-white rounded-2xl border border-slate-200 shadow-sm p-8">
      <form action="/admin/categories" method="POST" class="space-y-6">
        {{ csrfField() }}
        <div>
          <label for="name" class="block text-sm font-bold text-slate-700 mb-2">Nom de la catégorie</label>
          <input 
            type="text" 
            id="name" 
            name="name" 
            required
            class="w-full px-4 py-3 border border-slate-200 rounded-xl focus:ring-2 focus:ring-primary-500 focus:border-primary-500 transition-all"
            placeholder="ex: Développement Web, Design, Marketing..."
          />
          <p class="mt-2 text-xs text-slate-500">Le slug sera généré automatiquement à partir du nom.</p>
        </div>

        <div>
          <label for="icon" class="block text-sm font-bold text-slate-700 mb-2">Icône</label>
          <div class="flex gap-3">
            <input 
              type="text" 
              id="icon" 
              name="icon" 
              value="📚"
              class="flex-1 px-4 py-3 border border-slate-200 rounded-xl focus:ring-2 focus:ring-primary-500 focus:border-primary-500 transition-all"
              placeholder="📚"
            />
            <div class="flex gap-2">
              <button type="button" onclick="document.getElementById('icon').value='📚'" class="w-12 h-12 bg-slate-100 rounded-lg flex items-center justify-center hover:bg-slate-200 transition-colors text-xl">📚</button>
              <button type="button" onclick="document.getElementById('icon').value='💻'" class="w-12 h-12 bg-slate-100 rounded-lg flex items-center justify-center hover:bg-slate-200 transition-colors text-xl">💻</button>
              <button type="button" onclick="document.getElementById('icon').value='🎨'" class="w-12 h-12 bg-slate-100 rounded-lg flex items-center justify-center hover:bg-slate-200 transition-colors text-xl">🎨</button>
              <button type="button" onclick="document.getElementById('icon').value='📱'" class="w-12 h-12 bg-slate-100 rounded-lg flex items-center justify-center hover:bg-slate-200 transition-colors text-xl">📱</button>
              <button type="button" onclick="document.getElementById('icon').value='🔬'" class="w-12 h-12 bg-slate-100 rounded-lg flex items-center justify-center hover:bg-slate-200 transition-colors text-xl">🔬</button>
              <button type="button" onclick="document.getElementById('icon').value='📈'" class="w-12 h-12 bg-slate-100 rounded-lg flex items-center justify-center hover:bg-slate-200 transition-colors text-xl">📈</button>
            </div>
          </div>
          <p class="mt-2 text-xs text-slate-500">Choisissez une icône qui représente bien cette catégorie.</p>
        </div>

        <div>
          <label for="color" class="block text-sm font-bold text-slate-700 mb-2">Couleur</label>
          <div class="flex gap-3">
            <input 
              type="text" 
              id="color" 
              name="color" 
              value="#6366f1"
              pattern="^#[0-9A-Fa-f]{6}$"
              class="flex-1 px-4 py-3 border border-slate-200 rounded-xl focus:ring-2 focus:ring-primary-500 focus:border-primary-500 transition-all font-mono"
              placeholder="#6366f1"
            />
            <input 
              type="color" 
              id="colorPicker" 
              value="#6366f1"
              class="w-20 h-12 border border-slate-200 rounded-xl cursor-pointer"
              onchange="document.getElementById('color').value = this.value"
            />
          </div>
          <p class="mt-2 text-xs text-slate-500">Cette couleur sera utilisée pour l'identité visuelle de la catégorie.</p>
        </div>

        <div class="flex gap-4 pt-6 border-t border-slate-100">
          <a href="/admin/categories" class="flex-1 text-center px-6 py-3 bg-slate-100 text-slate-700 font-medium rounded-xl hover:bg-slate-200 transition-colors">
            Annuler
          </a>
          <button type="submit" class="flex-1 px-6 py-3 bg-primary-600 text-white font-medium rounded-xl hover:bg-primary-700 transition-colors shadow-lg">
            Créer la catégorie
          </button>
        </div>
      </form>
    </div>
  </div>

  <script>
    // Sync color picker with text input
    document.getElementById('color').addEventListener('input', function() {
      if (/^#[0-9A-Fa-f]{6}$/.test(this.value)) {
        document.getElementById('colorPicker').value = this.value;
      }
    });
  </script>
@end
